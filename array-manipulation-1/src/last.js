/* exported last */
// Psuedocode
// Define a function named last, taking one parameter named array
// within this function we need to return the last index of our array parameter.
// write a return statement that uses the array parameter at the index of the length of our array (use the length property) minus 1

function last(array) {
  return array[array.length - 1];
}
